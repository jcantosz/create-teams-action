{
  "scripts": {
    "build": "ncc build index.js --license licenses.txt",
    "test": "NODE_DEV=TEST jest",
    "coverage": "NODE_DEV=TEST jest --coverage --coverageReporters=text"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@octokit/auth-app": "^6.1.1",
    "@octokit/plugin-paginate-rest": "^9.1.4",
    "@octokit/plugin-throttling": "^8.1.3",
    "@octokit/rest": "^20.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
